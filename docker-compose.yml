services:
  db:
    restart: unless-stopped
    container_name: athleta_db
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: athleta
    ports:
      - "127.0.0.1:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j:
    restart: unless-stopped
    container_name: athleta_neo4j
    image: neo4j:5.26-community
    environment:
      NEO4J_AUTH: ${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-password}
      NEO4J_dbms_memory_pagecache_size: 512M
      NEO4J_dbms_memory_heap_initial__size: 512M
      NEO4J_dbms_memory_heap_max__size: 1G
    ports:
      - "127.0.0.1:7474:7474" # HTTP
      - "127.0.0.1:7687:7687" # Bolt
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  ai-engine:
    container_name: athleta_ai_engine
    build:
      context: ./services/ai-engine
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    restart: unless-stopped
    depends_on:
      - db
      - exercise-inference

  api:
    container_name: athleta_api
    build:
      context: ./services/api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped
    depends_on:
      - ai-engine
      - exercise-inference

  exercise-inference:
    container_name: athleta_exercise_inference
    build:
      context: ./services/exercise-inference
      dockerfile: Dockerfile
    environment:
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: ${NEO4J_USER:-neo4j}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-password}
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: athleta
      GRPC_PORT: 50051
    ports:
      - "50051:50051"
    depends_on:
      db:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      muscle-image-api:
        condition: service_healthy
    restart: unless-stopped

  muscle-image-api:
    container_name: muscle-image-api
    build:
      context: ./services/muscle-image
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    restart: unless-stopped

volumes:
  postgres_data:
  neo4j_data:
  neo4j_logs:
